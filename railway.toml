[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Healthcheck disabled temporarily to see full startup logs
# healthcheckPath = "/api/health"
# healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
